#ifndef pepita_common_h
#define pepita_common_h

/**
 * @brief Centralized include for commonly used types and constants.
 *
 * @details This file provides a convenient location for types and constants
 * used throughout pepita. It currently includes:
 *   - The `NULL` constant
 *   - The `size_t` type, often used for sizes and indices
 *   - The C99 `bool` type for clear Boolean semantics
 *   - Explicit-sized integer types, like `uint8_t`
 */
#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

#endif
